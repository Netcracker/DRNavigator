{{- if .Values.tls.generateCerts.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: site-manager-tls-certificate
  namespace: {{ .Release.Namespace }}
spec:
  secretName: sm-certs
  duration: {{ default 365 .Values.tls.generateCerts.duration | mul 24 }}h
  commonName: site-manager-ca
  isCA: false
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
  dnsNames:
{{ ( include "site-manager.certDnsNames" . | indent 4) }}
  ipAddresses:
{{ ( include "site-manager.certIpAddresses" . | indent 4) }}
  issuerRef:
    name: {{ .Values.tls.generateCerts.issuerName }}
    kind: {{ .Values.tls.generateCerts.issuerType }}
    group: cert-manager.io
{{- end }}