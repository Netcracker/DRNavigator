{{- if and (.Values.metricCollector.install) }}
apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  name: site-manager-grafana-dashboard
  labels:
    app: grafana
spec:
  name: dashboard-with-plugins.json
  json: >
{{ .Files.Get "templates/grafana-dashboard.json" | indent 4 }}
{{ end }}
