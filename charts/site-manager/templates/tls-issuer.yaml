{{- if and .Values.global.tls.generateCerts.enabled (not .Values.global.tls.generateCerts.clusterIssuerName) }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: site-manager-tls-issuer
  namespace: {{ .Release.Namespace }}
spec:
  selfSigned: {}
{{- end }}
